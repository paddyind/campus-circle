<?xml version="1.0" encoding="utf-8"?>
<!-- Allow HTTP (cleartext) so the app can reach http://YOUR_IP:8000/api on same WiFi. Chrome allows it; WebView blocks it without this. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
